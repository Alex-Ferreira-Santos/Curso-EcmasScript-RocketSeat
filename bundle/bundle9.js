"use strict";

var nome = "Alex";
var idade = 17; // não é necessario usar o nome:nome pois tem o mesmo nome, para simplificar pode-se apenas colocar o nome da variavel

var usuario = {
  nome: nome,
  idade: idade,
  cidade: "Rio"
};
console.log(usuario);
