const nome = "Alex"
const idade = 17

// não é necessario usar o nome:nome pois tem o mesmo nome, para simplificar pode-se apenas colocar o nome da variavel
const usuario = {
    nome,
    idade,
    cidade:"Rio"
}
console.log(usuario)